 const my_routes = {
    BASE:'/base',
    HOME:'/home'
}
const my_route_names = {
    BASE:'base',
    HOME:'home'
}
export const MY_ROUTES = Object.freeze(my_routes);
export const MY_ROUTE_NAMES = Object.freeze(my_route_names)